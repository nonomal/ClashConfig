{
  "name": "proxy",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "esbuild --bundle --outfile=./dist/index.js --platform=node --target=node20 ./index.ts",
    "deploy": "dotenv -e .env -- s deploy -y"
  },
  "packageManager": "pnpm@10.17.1",
  "dependencies": {
    "hono": "^4.10.7",
    "hono-alibaba-cloud-fc3-adapter": "^1.0.2"
  },
  "devDependencies": {
    "@serverless-devs/s": "^3.1.10",
    "dotenv-cli": "^11.0.0",
    "esbuild": "^0.27.0"
  }
}
